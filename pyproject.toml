[project]
name = "smiles-iupac-engine-prototype"
version = "0.1.0"
description = "Scoped deterministic SMILES-to-IUPAC naming engine prototype"
requires-python = ">=3.10"
dependencies = ["rdkit>=2025.3"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
